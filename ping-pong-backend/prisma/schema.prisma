
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PongGame {
  id Int @id @default(autoincrement())
  gameId String
  AdminId String
  MeetId String
  AdminScore String
  MeetScore String
}